# Walkthrough — F1.0 ציור אחד למסך הגלוי

אחרי שמירת הפניית אלמנטרי / שחזור פרמיה, נצבע רק המסך שפתוח (`LiveRefresh.renderActiveView`), פעם אחת ב־rAF.

לא שינינו כניסה, חיפוש ברשימה, או כפתור רענון. בדיקה: סנכרון + כניסה.
